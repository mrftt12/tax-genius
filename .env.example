# API Configuration
VITE_API_URL=http://localhost:8000/api
VITE_API_TIMEOUT=30000

# AI Service Configuration (if needed)
VITE_AI_API_KEY=your_ai_api_key_here
VITE_AI_MODEL=gpt-4

# Feature Flags
VITE_ENABLE_AI_ASSISTANT=true
VITE_ENABLE_DOCUMENT_UPLOAD=true

# Environment
VITE_ENV=development

# Supabase (client-side, safe-to-expose anon key; DO NOT include service_role here)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Default bucket name for document storage
VITE_SUPABASE_BUCKET=storage

# Notes:
# - Create a local file named `.env.local` for any secrets you do NOT want committed.
# - .gitignore already excludes .env, .env.local, and other env variants from version control.
# - Never put Supabase service_role or storage access keys in client env files; those belong on a server.
